# FluentShell Backlog

> 本文件记录第一版之后的迭代点。条目进入 Backlog 不代表已经承诺具体版本；实施前仍需单独设计、评估和验收。  
> 最后更新：2026-07-18

## 优先级说明

- `P1`：第一版稳定后优先评估，直接改善核心 SSH/SFTP 使用体验。
- `P2`：重要增强，需要新增较完整的交互或协议设计。
- `P3`：长期能力或平台化需求。

## 第一版收尾清单

以下项目不会以无效控件出现在当前界面中，但在发布 `1.0` 前需要继续验证或补齐：

- 使用专用 Linux 测试机覆盖密码、私钥、无口令私钥、错误凭据和指纹变化场景。
- 为服务器表单增加私钥文件选择器、字段即时校验和重复配置提示。
- 为主机指纹增加独立查看、复制、移除和重新确认入口。
- SFTP 传输显示已传字节、百分比和实时速度；当前基础版只显示进行状态和取消操作。
- SFTP 多文件传输增加可见的顺序队列；当前基础版只在会话内顺序执行。
- 设置页增加“仅清除凭据”操作，并将服务器配置、设置和凭据的清理范围分开说明。
- 补齐浅色、深色、高对比度、100%/150%/200% DPI 和窄窗口的完整视觉回归。
- 增加数据存储、指标解析、路径拼接和连接生命周期的自动化测试。

## P1：核心能力增强

### 终端模拟器增强（基础版已接入 xterm.js）

基础版已使用 xterm.js 渲染终端、转发原始 ANSI/VT 输出、接收键盘输入，并在窗口变化时同步远程 PTY 尺寸。以下是后续增强点：

- 支持 `vim`、`nano`、`less`、`top`、`htop` 等全屏 TUI。
- 支持鼠标事件、备用屏幕缓冲区和终端选择规则。
- 支持更多组合键、功能键和可配置键盘映射。
- 启用 URL 识别、可点击链接和终端内搜索 addon。
- 终端渲染性能、字体回退和高 DPI 适配专项验证。

### 自动重连

- 设置页增加“自动重连”Switch；功能完成前不显示无效开关。
- 可配置最大重试次数和重试间隔。
- 使用指数退避，支持用户立即取消。
- 监听本机网络恢复后再重试。
- 分别处理 SSH、Shell 和 SFTP 的恢复结果。
- 重连后尽可能恢复 SFTP 路径和工作区状态。

### SFTP 传输队列

- 多任务传输队列。
- 并行任务数量设置。
- 暂停、继续和取消。
- 失败重试和失败任务重新入队。
- 上传与下载断点续传；需要先核实 SSH.NET 和目标服务器能力。
- 传输速度、剩余时间和总进度。
- 上传/下载限速。
- 完成、失败和冲突通知。
- 关闭应用前处理未完成任务。

### 目录传输

- 递归上传本地目录。
- 递归下载远程目录。
- 目录覆盖和冲突策略。
- 符号链接循环检测。
- 大目录扫描进度和取消。

### 终端与 SFTP 路径协作

- “在 SFTP 中打开终端当前目录”。
- “在终端中进入当前 SFTP 目录”。
- 可选的目录自动同步。
- 明确处理 shell 别名、权限、软链接和无法可靠获取 cwd 的情况。

### 服务器配置迁移

- 导出不含密码和私钥口令的服务器配置。
- 导入服务器配置并处理重复项。
- 导入后提示重新输入凭据。
- 加密备份格式。
- 备份版本兼容和迁移。

### 命令历史与会话日志

- 可选持久化命令历史，默认关闭。
- 按服务器隔离历史。
- 敏感命令排除规则。
- 清理周期和一键清空。
- 导出当前会话日志。
- 日志脱敏和最大文件大小限制。

## P2：服务器与网络管理

### 高级认证

- SSH Agent。
- Windows OpenSSH Agent 集成。
- Keyboard-interactive 认证。
- 双因素认证流程。
- FIDO2/硬件安全密钥可行性评估。
- 多私钥候选和认证顺序。

### 跳板机、代理与隧道

- Jump Host / ProxyJump。
- 多级跳板链路。
- SOCKS5 代理。
- HTTP CONNECT 代理。
- 本地端口转发。
- 远程端口转发。
- 动态端口转发。
- 隧道状态、流量和关闭确认。

### 服务器组织

- 分组和文件夹树。
- 收藏服务器。
- 标签与筛选。
- 拖拽排序和跨组移动。
- 批量编辑服务器属性。
- 批量连接和批量断开。
- 服务器模板。

### 多服务器协作

- 向多个选定会话广播命令。
- 广播前显示目标服务器并要求确认。
- 多终端分屏。
- 会话标签拖动排序。
- 标签固定、静音和批量关闭。
- 工作区保存与恢复。

## P2：SFTP 文件管理增强

基础版远程单栏已改用 WinUI.TableView，具备列排序、筛选、列宽调整、列重排、双击进入目录、`..` 返回上级、可编辑路径和右键操作；操作命令已收纳到 SFTP 标题行。以下为后续增强点：

### 双栏文件管理器

- 本地目录和远程目录双栏布局。
- 两侧路径栏、历史和收藏位置。
- 本地与远程之间拖拽上传/下载。
- 键盘操作和焦点切换。
- 双栏宽度和布局持久化。

### 高级文件操作

- 非空目录递归删除。
- 删除到可恢复区域的可行性评估。
- 批量重命名。
- 文件与目录权限编辑（chmod）。
- 所有者与用户组编辑（chown/chgrp）。
- 创建和识别符号链接。
- 远程文件搜索。
- 按名称、大小、时间和类型进行高级筛选。
- 文件校验和比较。

### 文件查看与编辑

- 文本文件预览。
- 图片和常见文件类型预览。
- 远程文本编辑并安全上传替换。
- 外部编辑器集成。
- 修改冲突检测。
- 大文件预览限制。

## P2：监控增强

- CPU、内存和 Swap 历史曲线。
- 网络上行/下行吞吐量。
- 磁盘和挂载点使用情况。
- Load Average 历史。
- CPU 和内存占用最高的进程。
- 进程查看、筛选和安全终止。
- systemd 服务状态与启停。
- Docker 容器状态。
- 监控刷新周期设置。
- 资源异常阈值和通知。

## P2：平台兼容

- Windows OpenSSH 服务器的系统监控。
- NAS 设备适配。
- 路由器、交换机等网络设备的命令行兼容。
- BSD/macOS 远端监控。
- 不同 Shell（bash、zsh、fish、PowerShell、cmd）的行为适配。
- 非 UTF-8 远端编码。

## P2：终端效率工具

- 保存常用命令与命令片段。
- 命令参数模板。
- 服务器级和全局级快捷命令。
- 命令面板。
- 输出内容搜索增强。
- 正则搜索与结果导航。
- 终端标签内查找。
- 自定义快捷键。
- 右键快捷操作。

## P2：设置与外观

- 终端配色方案编辑。
- 自定义等距字体和字体回退。
- 光标样式与闪烁设置。
- 可配置快捷键。
- sidebar 宽度和各面板尺寸的高级设置。
- 更完整的通知设置。
- 代理设置。

## P3：长期能力

### 会话恢复与启动行为

- 记住上次打开的标签。
- 应用启动后可选恢复工作区。
- 可选“启动后自动连接”Switch，默认关闭；第一版始终只打开概览，不自动连接任何服务器。
- 与自动重连设置联动。
- 恢复前显示将要连接的服务器列表。

### 同步与备份

- 跨设备同步服务器配置。
- 端到端加密的凭据同步可行性评估。
- 云端备份历史和冲突处理。
- 配置版本回滚。

### 多语言

- English UI。
- UI 文案资源化。
- 跟随系统语言或手动选择。
- 日期、时间和文件大小本地化。

### 更新与发布

- 自动检查更新。
- 稳定版与预览版更新通道。
- MSIX 发布与签名流程。
- 崩溃报告和用户可控的诊断日志。
- 便携版/非打包部署可行性评估。

## Backlog 维护规则

- 新条目必须说明用户问题，不以“看起来高级”为理由加入。
- 安全、数据删除、外部连接和后台行为必须单独写验收标准。
- 功能真正进入实施阶段时，从本文件拆分为独立设计计划。
- 未实现的条目不得以无效按钮、禁用开关或“即将推出”徽章占据第一版界面。
- 完成条目后记录对应版本和实现文档，而不是直接无痕删除。
