# NovaShell 第一版实施计划

> 状态：规划已确认，尚未开始功能实现  
> 最后更新：2026-07-18

## 1. 产品目标

NovaShell 是一个使用 .NET 8 和 WinUI 3 构建的 Windows SSH/SFTP 客户端。第一版面向日常 Linux 服务器管理，重点解决以下问题：

- 在本机保存并管理多个服务器配置。
- 同时连接多台服务器，并在标签之间保持会话状态。
- 在同一工作区中使用 SSH 终端和远程 SFTP 文件管理器。
- 查看当前服务器的基础资源状态。
- 使用 Windows 原生能力安全保存密码和私钥口令。

第一版追求稳定、清晰、可维护，不以复刻 FinalShell 的全部功能为目标。

## 2. 已确认的产品原则

- 界面语言固定为简体中文。
- 第一版重点支持常见 Linux 发行版；其他 SSH 目标不保证监控信息完整。
- 不显示没有真实来源的统计数字或装饰性状态。
- 未实现的功能不提前放置无效开关或占位按钮。
- 危险操作需要确认；密码、口令和敏感历史不写入普通配置文件。
- 多服务器标签切换只改变当前视图，不主动断开后台会话。
- 终端、SFTP 和监控都属于当前连接标签，切换标签时整体同步切换。

## 3. 应用状态模型

应用只有两个顶层状态。

### 3.1 未连接状态

左侧为可收缩的全局 sidebar：

- 概览
- 已保存的服务器
- 设置（固定在底部）

右侧显示当前导航页面。

### 3.2 连接工作区

成功建立第一个连接后，原 sidebar 内容直接替换为当前服务器信息。连接状态下不提供返回概览或服务器列表的按钮。

- 顶部连接标签栏用于切换多个服务器会话。
- 标签栏末尾的 `+` 用于从已保存服务器中建立新连接或新增服务器。
- 只要还有连接标签，就保持在连接工作区。
- 关闭最后一个连接标签后，自动回到“已保存的服务器”页面。

```text
┌──────────────────────┬────────────────────────────────────┐
│ 当前服务器信息       │ [生产服务器] [测试服务器] [+]       │
│                      ├────────────────────────────────────┤
│ CPU                  │                                    │
│ 内存                 │              SSH 终端               │
│ Swap                 │                                    │
│ Load Average         │                                    │
│                      ├──────────── 可拖动分隔线 ───────────┤
│                      │              SFTP 文件管理          │
└──────────────────────┴────────────────────────────────────┘
```

## 4. 页面与交互范围

### 4.1 概览

概览只展示本地真实数据：

- 已保存服务器数量。
- 最近连接过的服务器。
- 最近一次连接结果。
- 新增服务器入口。
- 打开已保存服务器入口。
- 没有服务器时的空状态。

### 4.2 已保存的服务器

第一版使用单层列表，不做树形分组。

- 显示服务器名称、地址、端口、登录用户和最近连接时间。
- 支持搜索。
- 支持按名称或最近连接时间排序。
- 支持新增、编辑、复制和删除。
- 新增服务器窗口提供“保存”和“保存并连接”。

服务器表单字段：

- 服务器名称。
- 主机地址。
- SSH 端口，默认 `22`。
- 用户名。
- 密码或私钥认证方式。
- 私钥文件路径及可选口令。
- 是否记住凭据。
- 备注。

### 4.3 设置

第一版设置项：

- 浅色、深色或跟随系统主题。
- 终端字体大小。
- SFTP 默认下载目录。
- 是否保存成功登录后的凭据；默认关闭，用户必须主动选择。
- 清空本机保存的服务器和凭据。
- 查看本地数据位置。

### 4.4 连接标签

- 每个标签代表一个独立服务器会话。
- 标签显示服务器名称和真实连接状态。
- 切换标签不掉线。
- 非当前标签保持 SSH/SFTP 状态，但暂停资源监控轮询。
- 切回标签时立即刷新监控数据。
- 关闭标签才断开对应连接。
- 关闭正在传输文件的标签时必须二次确认。

### 4.5 连接态 sidebar

展开时显示：

- 服务器名称与连接状态。
- 主机地址、端口和登录用户。
- 操作系统与主机名。
- 系统运行时间。
- CPU 占用。
- 内存占用及已用/总量。
- Swap 占用及已用/总量。
- Load Average。
- 最近刷新时间。

收缩时只显示：

- CPU 占用。
- 内存占用。
- Swap 占用。

第一版使用数字和进度条，不绘制历史曲线。建议每 3 秒刷新一次当前标签；应用失去前台焦点时可降低刷新频率。

### 4.6 SSH 终端

终端主体和底部悬浮输入框共享同一个 SSH Shell 会话，当前目录、环境变量和命令执行状态保持一致。

终端主体第一版支持：

- 点击后直接输入普通 Shell 命令。
- Enter、Backspace、方向键和 Ctrl+C 等基础键位。
- 输出滚动、文本选择、复制和粘贴。
- 清空本地显示缓冲区，但不重建远程会话。

底部悬浮输入框第一版支持：

- 默认常驻终端底部。
- Enter 发送，Shift+Enter 换行。
- 展开为多行输入。
- 当前会话内的命令历史和搜索。
- 复制、粘贴与清空输入。
- 隐藏输入模式；隐藏内容不进入历史，发送后立即清空。
- 所有命令仍发送到终端主体使用的同一个 ShellStream。

边界：第一版不是完整终端模拟器，不保证 `vim`、`top`、`htop`、`nano`、`less` 等全屏 TUI 程序正确工作。终端输出只实现普通命令所需的基础 ANSI 处理。

### 4.7 SFTP 文件管理器

第一版只显示远程服务器单栏，不显示本地目录树。

- 显示远程路径栏。
- 双击进入目录，支持返回上级和刷新。
- 默认显示 Linux 隐藏文件，可在工具栏切换，并按服务器记住选择。
- 显示名称、类型、大小和修改时间。
- 上传一个或多个本地文件；通过 Windows 文件选择器选取。
- 下载选中的远程文件；通过 Windows 文件夹或保存位置选择器确定目标。
- 新建远程文件夹。
- 重命名文件或文件夹。
- 删除文件或空文件夹。
- 覆盖文件前二次确认。
- 终端路径和 SFTP 路径相互独立。

传输行为：

- SFTP 传输在后台执行，不阻塞 SSH 终端。
- 显示当前文件名、进度和取消按钮。
- 同一个服务器同时只处理一个传输任务；多个文件顺序执行。
- 切换连接标签不取消传输。

### 4.8 SSH/SFTP 布局

- SSH 默认占右侧工作区高度的约 65%。
- SFTP 默认占约 35%。
- 中间分隔线允许拖动。
- 将分隔线拖到接近窗口底部时，SFTP 自动吸附并收起。
- 收起后在终端底部保留明确的恢复入口。
- 重新展开时恢复上次使用的高度。

## 5. 安全与数据规则

### 5.1 凭据

- 密码和私钥口令使用 Windows Credential Locker。
- 只有成功登录并且用户选择记住时才保存凭据。
- 服务器 JSON 不包含密码或私钥口令。
- 删除服务器时同时询问是否删除对应凭据。

### 5.2 主机指纹

- 第一次连接显示 SSH 主机密钥指纹并要求确认。
- 确认后的指纹与服务器配置关联保存。
- 后续连接指纹不匹配时显示高优先级警告，不静默接受。

### 5.3 本地数据

- 服务器资料保存在 `%LOCALAPPDATA%\NovaShell\servers.json`。
- 首次运行时迁移旧版 `%LOCALAPPDATA%\SSHUI` 配置和 `SSHUI/` 凭据键；迁移失败时保留旧数据，不覆盖新数据。
- 设置保存在本机应用数据目录。
- 命令历史和终端输出只存在于当前运行会话，不写入磁盘。
- 第一版不导入、导出或云同步服务器数据。

## 6. 技术架构

### 6.1 技术选择

- 运行时：.NET 8。
- UI：WinUI 3 / Windows App SDK 稳定版。
- SSH/SFTP：SSH.NET。
- 凭据：`Windows.Security.Credentials.PasswordVault`。
- 配置序列化：`System.Text.Json`。
- 架构：轻量 MVVM；不额外引入 MVVM 框架。

截至 2026-07-18，实际使用：

- `Microsoft.WindowsAppSDK 2.3.1`
- `SSH.NET 2025.1.0`

Windows App SDK 采用自包含输出，避免目标机器缺少对应 Windows App Runtime 时出现 `REGDB_E_CLASSNOTREG`。

### 6.2 目录规划

```text
NovaShell/
├─ Models/
│  ├─ ServerProfile.cs
│  ├─ RemoteFileItem.cs
│  ├─ ServerMetrics.cs
│  └─ TransferTask.cs
├─ Services/
│  ├─ ServerProfileStore.cs
│  ├─ CredentialService.cs
│  ├─ SshSessionService.cs
│  ├─ SftpService.cs
│  ├─ ServerMetricsService.cs
│  └─ SettingsService.cs
├─ ViewModels/
│  ├─ MainViewModel.cs
│  ├─ OverviewViewModel.cs
│  ├─ ServerListViewModel.cs
│  ├─ ConnectionSessionViewModel.cs
│  └─ SettingsViewModel.cs
├─ Views/
│  ├─ OverviewPage.xaml
│  ├─ ServersPage.xaml
│  ├─ SettingsPage.xaml
│  ├─ ConnectionWorkspace.xaml
│  ├─ ServerSidebar.xaml
│  ├─ TerminalPanel.xaml
│  └─ SftpPanel.xaml
├─ PLAN.md
└─ BACKLOG.md
```

目录允许在实现中按实际复杂度合并，但必须保持凭据、连接协议和 UI 逻辑之间的边界。

### 6.3 会话模型

每个连接标签持有独立的 `ConnectionSessionViewModel`，其生命周期包含：

- 一个交互式 SSH Shell 会话。
- 一个 SFTP 会话。
- 当前终端输出缓冲区。
- 当前运行期间的命令历史。
- 当前 SFTP 路径和文件列表。
- 当前文件传输任务。
- 当前监控数据与刷新定时器。
- 当前连接状态和错误信息。

关闭标签时按顺序取消传输、停止监控、关闭 SFTP、关闭 Shell、释放 SSH 连接。

## 7. 实施阶段

### 阶段 0：修复项目基线

- 补齐缺失的 Windows App SDK 稳定版引用。
- 添加 SSH.NET 依赖。
- 清理模板生成的无用 using 和不需要的应用能力。
- 确保空白 WinUI 窗口可在 x64 Debug 下构建和启动。

完成状态：已补齐 SDK 与 SSH.NET 引用、移除无关 `systemAIModels` 能力，并通过 x64 Debug 构建和实际窗口启动验证。

### 当前实施快照（2026-07-18）

- 已完成：内容延伸式 WinUI 自定义标题栏、原生 Fluent `NavigationView` 外壳、概览、服务器列表、设置页和展开/收起状态。
- 已完成：服务器 JSON 存储、搜索、排序、新增、编辑、复制、删除，以及 Windows Credential Locker 接入。
- 已完成：主机指纹首次确认、已知指纹校验、密码和私钥认证基础流程。
- 已完成：多服务器 `TabView` 会话、最后标签关闭返回服务器列表、非当前标签暂停指标轮询。
- 已完成：ShellStream 普通命令输出、终端直接输入、增强输入、会话历史搜索和隐藏输入。
- 已完成：Linux CPU、内存、Swap、Load、主机名、系统和运行时间基础采集。
- 已完成：SFTP 单栏浏览、隐藏文件、新建目录、重命名、删除、上传、下载、覆盖确认、取消传输和拖动收起。
- 已验证：`dotnet build NovaShell.csproj -p:Platform=x64 --no-restore` 为 0 警告、0 错误；浅色常规窗口的概览、服务器页、设置页和侧栏状态已做实际窗口检查。
- 仍需真实环境验证：至少一台测试 Linux 主机上的密码认证、私钥认证、指纹变化、Shell 输出、指标采集和 SFTP 传输。

### 阶段 1：应用外壳与未连接状态

- 建立主题、颜色、排版和控件尺寸资源。
- 实现可收缩 sidebar。
- 实现概览、服务器列表和设置导航。
- 完成窄窗口与常规桌面窗口布局验证。

### 阶段 2：服务器数据与凭据

- 实现服务器模型和 JSON 本地存储。
- 实现新增、编辑、复制、删除、搜索和排序。
- 接入 Windows Credential Locker。
- 实现主机指纹确认和变化告警。

### 阶段 3：多连接工作区

- 实现连接标签栏和 `+` 入口。
- 实现连接生命周期和状态提示。
- 实现 sidebar 内容替换。
- 验证标签切换保持连接，最后标签关闭后返回服务器列表。

### 阶段 4：基础 SSH 终端

- 建立 ShellStream 并持续读取输出。
- 实现终端主体基础输入。
- 实现底部悬浮增强输入框。
- 实现会话内历史、搜索、多行和隐藏输入。
- 为终端缓冲区设置合理上限，避免长时间运行造成内存无限增长。

### 阶段 5：Linux 服务器监控

- 读取主机名、系统版本和运行时间。
- 通过 `/proc/stat`、`/proc/meminfo`、`/proc/loadavg` 等标准来源计算资源数据。
- 实现当前标签轮询、非当前标签暂停和切回刷新。
- 处理权限不足、命令缺失和非 Linux 目标的降级状态。

### 阶段 6：SFTP 文件管理

- 实现单栏目录浏览和隐藏文件切换。
- 实现上传、下载、新建目录、重命名和安全删除。
- 实现顺序传输、进度、取消和关闭标签确认。
- 实现可拖动、可吸附收起的底部面板。

### 阶段 7：设置、错误处理与视觉验证

- 完成第一版设置项。
- 统一连接、认证、网络、文件权限和磁盘错误提示。
- 验证明暗主题、键盘导航、焦点状态、文本换行和高 DPI。
- 验证常规桌面窗口和最小支持窗口宽度。
- 对关键危险操作进行确认测试。

## 8. 第一版验收标准

- 项目在 x64 Debug 下构建通过并能启动。
- 至少能保存并搜索多个服务器配置。
- 密码登录和带/不带口令的私钥登录均有明确流程。
- 首次主机指纹确认和指纹变化拦截可验证。
- 至少两个服务器标签可同时保持连接并独立保存界面状态。
- 终端主体和悬浮输入框可在同一 Shell 会话中连续执行普通命令。
- 当前标签可以显示可靠的 Linux CPU、内存、Swap 和 Load Average。
- SFTP 可以完成目录浏览、上传、下载、新建目录、重命名和安全删除。
- 文件传输时终端仍可使用，切换标签不会取消传输。
- SFTP 面板可以拖动、吸附收起和恢复。
- 关闭最后一个标签后回到已保存服务器页面。
- 重启应用不会自动连接服务器，终端输出和命令历史不会被持久化。
- 浅色、深色和跟随系统主题下均可读，键盘焦点和错误状态明确。

## 9. 非第一版范围

所有明确延后的能力统一维护在 [BACKLOG.md](BACKLOG.md)，第一版界面不放置不可用入口。
