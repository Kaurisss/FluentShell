<?xml version="1.0" encoding="utf-8"?>
<Application
    x:Class="FluentShell.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <controls:XamlControlsResources />
            </ResourceDictionary.MergedDictionaries>

            <ResourceDictionary.ThemeDictionaries>
                <ResourceDictionary x:Key="Light">
                    <SolidColorBrush x:Key="PageSurfaceBrush" Color="#B3F3F3F3" />
                    <SolidColorBrush x:Key="SidebarSurfaceBrush" Color="Transparent" />
                    <SolidColorBrush x:Key="PanelSurfaceBrush" Color="#CCFFFFFF" />
                    <SolidColorBrush x:Key="MutedTextBrush" Color="#5C5C5C" />
                    <SolidColorBrush x:Key="SubtleStrokeBrush" Color="#18000000" />
                    <SolidColorBrush x:Key="NavigationViewDefaultPaneBackground" Color="Transparent" />
                    <SolidColorBrush x:Key="NavigationViewExpandedPaneBackground" Color="Transparent" />
                </ResourceDictionary>
                <ResourceDictionary x:Key="Dark">
                    <SolidColorBrush x:Key="PageSurfaceBrush" Color="#B3202020" />
                    <SolidColorBrush x:Key="SidebarSurfaceBrush" Color="Transparent" />
                    <SolidColorBrush x:Key="PanelSurfaceBrush" Color="#CC2B2B2B" />
                    <SolidColorBrush x:Key="MutedTextBrush" Color="#B5B5B5" />
                    <SolidColorBrush x:Key="SubtleStrokeBrush" Color="#26FFFFFF" />
                    <SolidColorBrush x:Key="NavigationViewDefaultPaneBackground" Color="Transparent" />
                    <SolidColorBrush x:Key="NavigationViewExpandedPaneBackground" Color="Transparent" />
                </ResourceDictionary>
                <ResourceDictionary x:Key="HighContrast">
                    <SolidColorBrush x:Key="PageSurfaceBrush" Color="{ThemeResource SystemColorWindowColor}" />
                    <SolidColorBrush x:Key="SidebarSurfaceBrush" Color="{ThemeResource SystemColorWindowColor}" />
                    <SolidColorBrush x:Key="PanelSurfaceBrush" Color="{ThemeResource SystemColorWindowColor}" />
                    <SolidColorBrush x:Key="MutedTextBrush" Color="{ThemeResource SystemColorWindowTextColor}" />
                    <SolidColorBrush x:Key="SubtleStrokeBrush" Color="{ThemeResource SystemColorWindowTextColor}" />
                    <SolidColorBrush x:Key="NavigationViewDefaultPaneBackground" Color="{ThemeResource SystemColorWindowColor}" />
                    <SolidColorBrush x:Key="NavigationViewExpandedPaneBackground" Color="{ThemeResource SystemColorWindowColor}" />
                </ResourceDictionary>
            </ResourceDictionary.ThemeDictionaries>

            <SolidColorBrush x:Key="TerminalSurfaceBrush" Color="#101214" />
            <SolidColorBrush x:Key="TerminalTextBrush" Color="#E6EDF3" />
            <x:Double x:Key="IconButtonSize">40</x:Double>
            <Thickness x:Key="AppBarButtonInnerBorderMargin">2</Thickness>
            <Thickness x:Key="AppBarButtonContentViewboxCollapsedMargin">0,12</Thickness>

            <StaticResource x:Key="AppBarButtonBackground" ResourceKey="ButtonBackground" />
            <StaticResource x:Key="AppBarButtonBackgroundPointerOver" ResourceKey="ButtonBackgroundPointerOver" />
            <StaticResource x:Key="AppBarButtonBackgroundPressed" ResourceKey="ButtonBackgroundPressed" />
            <StaticResource x:Key="AppBarButtonBackgroundDisabled" ResourceKey="ButtonBackgroundDisabled" />
            <StaticResource x:Key="AppBarButtonForeground" ResourceKey="ButtonForeground" />
            <StaticResource x:Key="AppBarButtonForegroundPointerOver" ResourceKey="ButtonForegroundPointerOver" />
            <StaticResource x:Key="AppBarButtonForegroundPressed" ResourceKey="ButtonForegroundPressed" />
            <StaticResource x:Key="AppBarButtonForegroundDisabled" ResourceKey="ButtonForegroundDisabled" />
            <StaticResource x:Key="AppBarButtonBorderBrush" ResourceKey="ButtonBorderBrush" />
            <StaticResource x:Key="AppBarButtonBorderBrushPointerOver" ResourceKey="ButtonBorderBrushPointerOver" />
            <StaticResource x:Key="AppBarButtonBorderBrushPressed" ResourceKey="ButtonBorderBrushPressed" />
            <StaticResource x:Key="AppBarButtonBorderBrushDisabled" ResourceKey="ButtonBorderBrushDisabled" />

            <Style TargetType="Button" BasedOn="{StaticResource DefaultButtonStyle}">
                <Setter Property="CornerRadius" Value="{ThemeResource ControlCornerRadius}" />
            </Style>
            <Style x:Key="AppAccentButtonStyle" TargetType="Button" BasedOn="{StaticResource AccentButtonStyle}">
                <Setter Property="CornerRadius" Value="{ThemeResource ControlCornerRadius}" />
            </Style>

            <Style x:Key="PageHeadingStyle" TargetType="TextBlock">
                <Setter Property="FontFamily" Value="Segoe UI Variable Display" />
                <Setter Property="FontSize" Value="28" />
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="TextWrapping" Value="Wrap" />
            </Style>
            <Style x:Key="SectionHeadingStyle" TargetType="TextBlock">
                <Setter Property="FontFamily" Value="Segoe UI Variable Text" />
                <Setter Property="FontSize" Value="18" />
                <Setter Property="FontWeight" Value="SemiBold" />
            </Style>

            <Style x:Key="TitleBarSessionTabStyle" TargetType="ToggleButton">
                <Setter Property="Height" Value="40" />
                <Setter Property="MinWidth" Value="132" />
                <Setter Property="Padding" Value="12,0,40,0" />
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="Foreground" Value="{ThemeResource TextFillColorPrimaryBrush}" />
                <Setter Property="HorizontalContentAlignment" Value="Left" />
                <Setter Property="VerticalContentAlignment" Value="Center" />
                <Setter Property="UseSystemFocusVisuals" Value="True" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ToggleButton">
                            <Grid>
                                <VisualStateManager.VisualStateGroups>
                                    <VisualStateGroup x:Name="CommonStates">
                                        <VisualState x:Name="Normal" />
                                        <VisualState x:Name="PointerOver">
                                            <VisualState.Setters>
                                                <Setter Target="TabBackground.Background" Value="{ThemeResource ControlFillColorSecondaryBrush}" />
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="Pressed">
                                            <VisualState.Setters>
                                                <Setter Target="TabBackground.Background" Value="{ThemeResource ControlFillColorTertiaryBrush}" />
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="Disabled">
                                            <VisualState.Setters>
                                                <Setter Target="ContentPresenter.Opacity" Value="0.36" />
                                            </VisualState.Setters>
                                        </VisualState>
                                    </VisualStateGroup>
                                    <VisualStateGroup x:Name="CheckStates">
                                        <VisualState x:Name="Unchecked" />
                                        <VisualState x:Name="Checked">
                                            <VisualState.Setters>
                                                <Setter Target="TabBackground.Background" Value="{ThemeResource ControlFillColorDefaultBrush}" />
                                                <Setter Target="TabBackground.BorderBrush" Value="{ThemeResource SubtleStrokeBrush}" />
                                                <Setter Target="TabBackground.BorderThickness" Value="1" />
                                            </VisualState.Setters>
                                        </VisualState>
                                    </VisualStateGroup>
                                </VisualStateManager.VisualStateGroups>
                                <Border
                                    x:Name="TabBackground"
                                    Margin="0,3,3,3"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="Transparent"
                                    BorderThickness="0"
                                    CornerRadius="8" />
                                <ContentPresenter
                                    x:Name="ContentPresenter"
                                    Padding="{TemplateBinding Padding}"
                                    HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                    VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"
                                    Content="{TemplateBinding Content}"
                                    ContentTemplate="{TemplateBinding ContentTemplate}"
                                    Foreground="{TemplateBinding Foreground}" />
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="IconButtonStyle" TargetType="Button">
                <Setter Property="Width" Value="{StaticResource IconButtonSize}" />
                <Setter Property="Height" Value="{StaticResource IconButtonSize}" />
                <Setter Property="Padding" Value="0" />
                <Setter Property="Background" Value="{ThemeResource ButtonBackground}" />
                <Setter Property="BorderBrush" Value="{ThemeResource ButtonBorderBrush}" />
                <Setter Property="BorderThickness" Value="{ThemeResource ButtonBorderThemeThickness}" />
                <Setter Property="Foreground" Value="{ThemeResource NavigationViewItemForeground}" />
                <Setter Property="UseSystemFocusVisuals" Value="True" />
                <Setter Property="FocusVisualMargin" Value="-3" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Grid>
                                <VisualStateManager.VisualStateGroups>
                                    <VisualStateGroup x:Name="CommonStates">
                                        <VisualState x:Name="Normal" />
                                        <VisualState x:Name="PointerOver">
                                            <VisualState.Setters>
                                                <Setter Target="ButtonBackground.Background" Value="{ThemeResource NavigationViewItemBackgroundPointerOver}" />
                                                <Setter Target="ButtonBackground.BorderBrush" Value="{ThemeResource ButtonBorderBrushPointerOver}" />
                                                <Setter Target="ButtonContent.Foreground" Value="{ThemeResource NavigationViewItemForegroundPointerOver}" />
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="Pressed">
                                            <VisualState.Setters>
                                                <Setter Target="ButtonBackground.Background" Value="{ThemeResource NavigationViewItemBackgroundPressed}" />
                                                <Setter Target="ButtonBackground.BorderBrush" Value="{ThemeResource ButtonBorderBrushPressed}" />
                                                <Setter Target="ButtonContent.Foreground" Value="{ThemeResource NavigationViewItemForegroundPressed}" />
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="Disabled">
                                            <VisualState.Setters>
                                                <Setter Target="ButtonBackground.Background" Value="{ThemeResource NavigationViewItemBackgroundDisabled}" />
                                                <Setter Target="ButtonBackground.BorderBrush" Value="{ThemeResource ButtonBorderBrushDisabled}" />
                                                <Setter Target="ButtonContent.Foreground" Value="{ThemeResource NavigationViewItemForegroundDisabled}" />
                                            </VisualState.Setters>
                                        </VisualState>
                                    </VisualStateGroup>
                                </VisualStateManager.VisualStateGroups>
                                <Border
                                    x:Name="ButtonBackground"
                                    Margin="0,4"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="{ThemeResource ControlCornerRadius}"
                                    Control.IsTemplateFocusTarget="True" />
                                <ContentPresenter
                                    x:Name="ButtonContent"
                                    HorizontalContentAlignment="Center"
                                    VerticalContentAlignment="Center"
                                    Content="{TemplateBinding Content}"
                                    ContentTemplate="{TemplateBinding ContentTemplate}"
                                    Foreground="{TemplateBinding Foreground}" />
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="CommandBarIconButtonStyle" TargetType="AppBarButton" BasedOn="{StaticResource DefaultAppBarButtonStyle}">
                <Setter Property="Width" Value="{StaticResource IconButtonSize}" />
                <Setter Property="Height" Value="{StaticResource IconButtonSize}" />
                <Setter Property="VerticalAlignment" Value="Center" />
                <Setter Property="Background" Value="{ThemeResource ButtonBackground}" />
                <Setter Property="BorderBrush" Value="{ThemeResource ButtonBorderBrush}" />
                <Setter Property="BorderThickness" Value="{ThemeResource ButtonBorderThemeThickness}" />
                <Setter Property="CornerRadius" Value="{ThemeResource ControlCornerRadius}" />
            </Style>

            <Style x:Key="WorkspaceSplitterStyle" TargetType="Thumb">
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="HorizontalAlignment" Value="Stretch" />
                <Setter Property="VerticalAlignment" Value="Stretch" />
                <Setter Property="IsTabStop" Value="False" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Thumb">
                            <!-- 整块是命中区域，中间那条线才是视觉上的拆分条 -->
                            <Grid Background="Transparent">
                                <VisualStateManager.VisualStateGroups>
                                    <VisualStateGroup x:Name="CommonStates">
                                        <VisualState x:Name="Normal" />
                                        <VisualState x:Name="PointerOver">
                                            <VisualState.Setters>
                                                <Setter Target="SplitterLine.Height" Value="3" />
                                                <Setter Target="SplitterLine.Background" Value="{ThemeResource AccentFillColorSecondaryBrush}" />
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="Pressed">
                                            <VisualState.Setters>
                                                <Setter Target="SplitterLine.Height" Value="3" />
                                                <Setter Target="SplitterLine.Background" Value="{ThemeResource AccentFillColorDefaultBrush}" />
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="Disabled" />
                                    </VisualStateGroup>
                                </VisualStateManager.VisualStateGroups>
                                <Border
                                    x:Name="SplitterLine"
                                    Height="1"
                                    VerticalAlignment="Center"
                                    Background="{ThemeResource SubtleStrokeBrush}"
                                    CornerRadius="1" />
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="TitleBarSessionIconButtonStyle" TargetType="Button" BasedOn="{StaticResource IconButtonStyle}">
                <Setter Property="VerticalAlignment" Value="Center" />
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="BorderThickness" Value="0" />
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
